{
  "name": "tuner",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lockfile:linux": "rm -f package-lock.json && npm install --package-lock-only --os=linux --cpu=x64 --libc=glibc --include=optional",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "pitchy": "^4.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.3.0",
    "vite": "^8.0.3",
    "vitest": "^4.1.2"
  }
}
